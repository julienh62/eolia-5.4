{% extends 'base.admin.html.twig' %}

{% block title %}New Activity{% endblock %}

{% block body %}
    <h1>Créer  Activity au planning</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

    <a href="{{ path('app_admin_activitie_index') }}">Retour à la liste</a>
{% endblock %}
