<form method="post" action="{{ path('app_category_color_setting_delete', {'id': category_color_setting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_color_setting.id) }}">
    <button class="btn">Delete</button>
</form>
