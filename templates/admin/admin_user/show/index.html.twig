{% extends 'base.html.twig' %}

{% block title %}adminusershow{% endblock %}

{% block body %}


    <div class="container">
     <h1>List of Users</h1>
    
    <ul>
        {% for user in users %}
            <li>
       

      <h2>{{ user.email }}</h2>
 {#}<a href="{{ path('showpurchase_user', {'status': purchase.status}) }}">{{ purchase.status }}</a>#}
      
            <a href="{{ path('all_user_show', {'fullName': user.fullName}) }}">{{ user.fullName }}</a>  
                {# {'name': tag.name} : correspond à au parametre id que l'on pase à la routeid   #}
             

    </li>
                
             {#   <p>Created at: {{ user.created_at|date('Y-m-d H:i:s') }}</p> #}
                <!-- Ajoutez d'autres informations de l'article si nécessaire -->
           
        {% endfor %}
    </ul>
    </div>
{% endblock %}





